import React from 'react';import{Button}from'./Card';export function Modal({open,onClose,title,children}:{open:boolean;onClose:()=>void;title:string;children:React.ReactNode}){if(!open)return null;return <div className='modal'><div className='modal-card'><div className='section-header'><h2>{title}</h2><Button variant='ghost' onClick={onClose}>Close</Button></div>{children}</div></div>}
