import{Search}from'lucide-react';import{PageKey,pageTitles}from'./Sidebar';
export function CommandPalette({open,onClose,setPage}:{open:boolean;onClose:()=>void;setPage:(p:PageKey)=>void}){if(!open)return null;const pages=Object.entries(pageTitles) as [PageKey,string][];return <div className='command-overlay' onClick={onClose}><div className='command-card' onClick={e=>e.stopPropagation()}><div className='command-input'><Search size={18}/><span>Jump to a room or workflow</span></div>{pages.map(([key,label])=><button key={key} onClick={()=>{setPage(key);onClose()}}><b>{label}</b><small>{key==='codex'?'start audited run / inspect sessions':key==='deepseek'?'chat threads and provider state':key==='claude'?'setup checklist':key==='hermes'?'gateway and operating layer':'workspace control plane'}</small></button>)}</div></div>}
