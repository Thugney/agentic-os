import{useState}from'react';import{Sidebar,PageKey}from'./Sidebar';import{TopBar}from'./TopBar';import{CommandPalette}from'./CommandPalette';import{MobileNav}from'./MobileNav';import{Drawer}from'../primitives/Drawer';import{Routes}from'../../app/routes';
export function AppShell(){const[page,setPage]=useState<PageKey>('mission');const[drawer,setDrawer]=useState(false);const[cmd,setCmd]=useState(false);function go(p:PageKey){setPage(p);setDrawer(false)}return <div className='app-shell'><div className='desktop-sidebar'><Sidebar page={page} setPage={go}/></div><Drawer open={drawer} onClose={()=>setDrawer(false)}><Sidebar page={page} setPage={go} compact/></Drawer><main className='main-stage'><TopBar page={page} onMenu={()=>setDrawer(true)} onCommand={()=>setCmd(true)} onNewTask={()=>go('kanban')}/><Routes page={page} setPage={go}/></main><MobileNav page={page} setPage={go}/><CommandPalette open={cmd} onClose={()=>setCmd(false)} setPage={go}/></div>}
